apply from: 'build.gradle'

version = '0.4.1'

publishing {
    publications {
        maven(MavenPublication) {
            artifactId = 'flipt-client-java-musl'
            from components.java
        }
    }
}
