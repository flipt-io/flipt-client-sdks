[tools]
python = "3.11"
poetry = "1.8.5"
black = "latest"

[tasks.lint]
description = "Run Python lint checks"
run = "poetry install && poetry run black --check ."

[tasks.fmt]
description = "Format Python source"
run = "poetry install && poetry run black ."
